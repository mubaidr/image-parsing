const state = {
  options: {
    source: '.',
    target: '.',
    imageFormats: [
      'png',
      'jpg',
      'jpeg',
      'jpe',
      'jfif',
      'gif',
      'tif',
      'tiff',
      'bmp',
      'dib'
    ]
  }
}

const getters = {}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
